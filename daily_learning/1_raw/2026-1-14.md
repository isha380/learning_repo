layout.tsx is a Server Component - it runs on the server where there's no localStorage or browser APIs
User data is in localStorage - we need client-side code to access the token and fetch user data
Navbar needs user prop - but we can't fetch it in the server component
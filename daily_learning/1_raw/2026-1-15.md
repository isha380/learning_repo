1. FormData vs JSON - When to use each:

Use FormData when:

--> Uploading files (images, PDFs, etc.)
--> Sending mixed data (text + files together)
--> Your current case - profile update with optional image

Use JSON when:

--> Pure text/data updates (no files)
--> APIs that need strict typing
--> Better for mobile apps and API integrations